apiVersion: v1
kind: Service

metadata:
  name: python-app-svc

spec:
  type: {{ .Values.service.type }}

  selector:
    app: python-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}

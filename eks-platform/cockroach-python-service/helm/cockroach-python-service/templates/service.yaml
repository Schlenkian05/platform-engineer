apiVersion: v1
kind: Service
metadata:
  name: {{ include "cockroach-python-service.fullname" . }}
  labels:
    {{- include "cockroach-python-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cockroach-python-service.selectorLabels" . | nindent 4 }}
